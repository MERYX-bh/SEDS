from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This a project for a school course',
    author='meriem baha',
    license='',
)
